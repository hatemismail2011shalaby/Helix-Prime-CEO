# HELIX CLEAN CODE CONSTITUTION

**Rule 1 (Self-Discovery):** The system never relies on hardcoded configs.
**Rule 2 (Dynamic Audit):** Before any task, the system verifies its own hardware and model availability.
**Rule 3 (Zero Manual Steps):** AI writes Python. Python executes folders. Human only supervises.
